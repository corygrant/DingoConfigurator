<UserControl x:Class="DingoConfigurator.Views.SoftButtonBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DingoConfigurator.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <DataTemplate x:Key="ButtonTemplate">
            <Button Content="{Binding Number}"
                    Command="{Binding DataContext.ButtonPressCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding Index}"
                    Margin="5"
                    Width="80"
                    Height="80"
                    Background="{Binding ButtonBackground}"
                    BorderBrush="{Binding ButtonBorder}"
                    BorderThickness="4">
            </Button>
        </DataTemplate>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <StackPanel>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Keypad Model:"
                           VerticalAlignment="Center" Margin="5"/>
                <ComboBox Grid.Row="0" Grid.Column="1"
                          ItemsSource="{Binding KeypadModels}"
                          SelectedItem="{Binding SelectedKeypadModel}"
                          HorizontalAlignment="Left"
                          HorizontalContentAlignment="Left"
                          MinWidth="100" Width="200" Margin="5"/>

                <TextBlock Grid.Row="0" Grid.Column="2" Text="Base CAN ID:"
                           VerticalAlignment="Center" Margin="5"/>
                <TextBox Grid.Row="0" Grid.Column="3"
                         Text="{Binding SoftBtnBox.BaseId}"
                         HorizontalAlignment="Left"
                         HorizontalContentAlignment="Center"
                         MinWidth="80" Width="80" Margin="5"/>
            </Grid>

            <Separator Margin="0 5 0 0" />

            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding ButtonStates}"
                              ItemTemplate="{StaticResource ButtonTemplate}" Margin="10">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="{Binding ButtonColumns}" HorizontalAlignment="Left"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>

            <GridSplitter HorizontalAlignment="Stretch"
                          VerticalAlignment="Center"
                          Background="White"
                          ShowsPreview="True"
                          Height="3"/>

            <StackPanel Visibility="{Binding HasDials, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ItemsControl ItemsSource="{Binding DialStates}" Margin="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,5">
                                <TextBlock Text="{Binding Number, StringFormat='Dial {0}:'}"
                                           Width="60" VerticalAlignment="Center"/>
                                <Slider Value="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                        Minimum="{Binding MinValue}"
                                        Maximum="{Binding MaxValue}"
                                        Width="200"
                                        Margin="10,0"
                                        TickFrequency="1000"
                                        IsSnapToTickEnabled="True"/>
                                <TextBox Text="{Binding Value}"
                                         Width="80"
                                         Margin="10,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
