<UserControl x:Class="DingoConfigurator.Views.Dialogs.ReadConfirmDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border BorderThickness="2" BorderBrush="Lime">
        <StackPanel>
            <TextBlock Text="Overwrite all values?" Margin="5"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Yes" Command="{Binding ReadBtnConfirmCmd}" Margin="5"/>
                <Button Content="No" Command="{Binding CancelConfirmCmd}" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
